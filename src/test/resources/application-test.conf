jwt {
  secret = "test-secret"
  issuer = "bookvault-test"
  audience = "bookvault-users-test"
}

database {
  url = "jdbc:postgresql://localhost:5432/postgres"
  user = "postgres"
  password = "postgres"
}